# 구현 핸드오프 — AI 홀덤

> 이 문서는 **제로 컨텍스트로 새로 시작하는 구현 세션**(1순위 상정: grok-4.6)을 위한 자족적 핸드오프다. 이 문서 → 스펙 → 플랜 순서로 읽으면 구현에 필요한 모든 컨텍스트가 갖춰진다.

## 1. 미션

AI 에이전트들과 웹 UI로 즐기는 노리밋 텍사스 홀덤(딜러 = AI 세션, 코칭 포함)의 **코드 전체를 구현**한다: 포커 엔진 CLI + 중계 웹 서버 + 웹 UI + 딜러 스킬 문서 + 전체 테스트. 게임을 실제로 플레이하는 것은 이 세션의 일이 아니다(§6 참고).

## 2. 읽기 순서 (필수)

1. 이 문서 전체
2. **스펙(정본)**: `docs/superpowers/specs/2026-08-26-ai-holdem-design.md` — 모든 계약(액션 스키마, envelope, view 필드, 서버 API, 무결성 계약)의 단일 정본
3. **플랜(작업 순서)**: `docs/superpowers/plans/2026-08-27-ai-holdem-implementation.md` — Task 0~14, 태스크별 파일·인터페이스·테스트·커밋

스펙은 2라운드 교차 모델 리뷰(grok-4.6 + gpt-5.6-sol)로 수렴 완료된 문서다. 플랜도 동일한 리뷰를 거쳤다. 문서를 "개선"하려 하지 말고 그대로 구현하라.

## 3. 실행 프로토콜

- Task 0부터 **번호 순서대로**. 각 태스크는 RED(실패 테스트 먼저) → GREEN(최소 구현) → 전체 테스트 그린(`node --test test/`) → 커밋.
- 진행 상황은 플랜 파일의 체크박스(`- [ ]` → `- [x]`)를 직접 갱신해 추적한다.
- 커밋: conventional commit(`feat:`/`test:`/`chore:`/`docs:`), 제목 한국어 허용, 태스크당 1커밋 이상, `main` 직접 커밋.
- 태스크를 건너뛰거나 병합하지 마라. 태스크 내부 스텝은 상황에 맞게 묶어도 되지만 "테스트 먼저"는 예외 없다.

## 4. 확정 제약 (재확인)

- Node ≥ 20 (이 머신은 v26, macOS), `"type":"module"` ESM, **외부 npm 의존성 0개**, 빌드 없음.
- 사용자 노출 문자열 전부 한국어.
- `engine/**`는 순수(네트워크·타이머·LLM 금지), 서버는 게임 로직 무지(엔진 import 금지).
- 카드 표기 `"As"/"Td"/"7c"`, 금액은 정수 칩, raise amount는 항상 raise-to.
- `game/` 디렉토리는 gitignore(런타임 상태) — 코드·테스트 산출물을 거기 두지 마라.

## 5. 판단 규칙 (막혔을 때)

1. **플랜과 스펙이 충돌하면 스펙이 이긴다.** 충돌 발견 시 스펙대로 구현하고 커밋 메시지에 한 줄 기록.
2. 두 문서 모두에 답이 없는 **사소한** 결정(변수명, 내부 구조, CSS 세부)은 더 단순한 쪽을 택하고 진행. 커밋 메시지에 결정을 남긴다.
3. **스펙의 계약(공개 인터페이스·규칙·필드)을 바꿔야만 풀리는 문제**를 만나면 멈추고 사용자에게 보고한다. 계약을 임의로 바꾸지 마라.
4. 스펙 §14(구현 검증으로의 이월)의 "기각" 항목을 되살리지 마라(예: 명령별 operationId 전면 멱등, 토큰 이원화). "이월" 항목은 플랜의 해당 테스트가 검증한다.
5. 포커 규칙이 헷갈리면: 스펙 §4 규칙 범위가 정의의 전부다. 그 범위를 넘는 변형 룰(앤티, 스트래들 등)은 §12 YAGNI — 구현하지 않는다.

## 6. 하지 말 것

- deep-loop 등 **오케스트레이션 플러그인 사용 금지** (사전 검토 결과: 이 작업 유형에 부적합 판정).
- 실제 LLM 플레이어 에이전트 스폰·게임 플레이 — 첫 게임은 나중에 **Claude Code 세션에서 `/start-game`으로** 실행한다(지속 에이전트가 있는 정본 런타임이라서다. 스펙 §9 v1 범위).
- Claude Code의 심볼릭 링크 스킬 인식 검증 — 이 세션에서 불가능. README의 "구현 후 확인 체크리스트"에 남기는 것까지가 임무다.
- 스펙 §12 YAGNI 목록의 어떤 것도 구현하지 않는다.
- 외부 npm 패키지 설치 금지 (`pokersolver` 등 유혹을 참아라 — 평가기는 플랜 Task 2의 참조 알고리즘으로 직접 구현).

## 7. 완료 기준

플랜의 "수용 기준" 5개가 전부 충족되면 완료다:

1. `node --test` 전부 그린 (엔진 단위 + 서버 + 시뮬레이션 불변식 — 인자 없이 실행. **`node --test test/`처럼 디렉토리 인자를 주면 Node v26에서 실패하므로 금지**)
2. `test/helpers/dev-drive.js` 시퀀스가 브라우저에서 스펙 §7 전 요소 시연
3. 시뮬레이션 불변식(칩 보존·종료 보장·음수 스택 금지) 3구성×3시드 통과
4. `game/` gitignore 유지, 시크릿·절대경로 커밋 없음
5. 스펙 §11 테스트 항목 전부가 `test/`에 실재

## 8. 완료 보고 형식

마지막 커밋 후 사용자에게 보고하라:

- 태스크별 완료 상태와 커밋 해시 목록
- 전체 테스트 결과 요약 (`node --test` 출력의 pass/fail 카운트)
- 구현 중 내린 판단 기록(§5 규칙 2·3에 해당했던 것)
- **사용자가 직접 할 일 안내**: ① Claude Code 세션을 이 저장소에서 열고 `/start-game` 인식 확인(안 되면 README 체크리스트의 폴백) ② 첫 게임 스모크 플레이 ③ Codex·Grok 스킬 경로 확인(README 체크리스트)

## 9. 환경 사실

- 저장소: `/Users/sungmin/Dev/AI-Holdem` (git, `main` 브랜치, 이 시점까지는 docs만 존재)
- 플랫폼: macOS (darwin), zsh, Node v26 설치됨
- 이 프로젝트의 배경 결정 이력이 필요하면 `git log docs/`의 커밋 메시지에 리뷰 라운드별 요약이 있다
